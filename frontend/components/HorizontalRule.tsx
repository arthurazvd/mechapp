import { View } from "react-native";

const HorizontalRule = () => (
  <View
    style={{
      borderBottomWidth: 1,
      borderBottomColor: "#ccc",
      marginVertical: 16,
      width: "100%",
    }}
  />
);

export default HorizontalRule;
